---
aci: Hydra (CMU, 1971–75)
axiom: CG1
node: The security lineage's great-grandparent — a capability-based microkernel whose 'separation of policy and mechanism' and whose capabilities (you touch only what you hold a token for) are the ancestor of allowlist-by-construction.
class: STOICHEION emergent
who: Hydra (CMU, 1971–75) — The security lineage's great-grandparent — a capability-based microkernel whose 'separation of policy and mechanism' and whose capabilities (you touch only what you hold a token for) are the ancestor of allowlist-by-construction.
what: The security lineage's great-grandparent — a capability-based microkernel whose 'separation of policy and mechanism' and whose capabilities (you touch only what you hold a token for) are the ancestor of allowlist-by-construction.
why: The security lineage's great-grandparent — a capability-based microkernel whose 'separation of policy and mechanism' and whose capabilities (you touch only what you hold a token for) are the ancestor of allowlist-by-construction.
how: The security lineage's great-grandparent — a capability-based microkernel whose 'separation of policy and mechanism' and whose capabilities (you touch only what you hold a token for) are the ancestor of allowlist-by-construction.
where: CG1 · the ladder of boxes  Framework inputs: kernel.org/NIST/Chromium docs + Bostrom/Yudkowsky/Alfonseca + Saltzer & Schroeder; dual-agent web-verified  Conductor / external anchor: ROOT0 (catalogued into UD0)
moniker: ⟦Hydra (CMU, 1971–75):CG1:f73651⟧
token: zys-zys-thur
silicon_badge: hydra.silicon.png
carbon_badge: hydra.carbon.tiff
spun: hydra.spun
credit: hydra.1099
seal: The security lineage's great-grandparent — a capability-based microkernel whose 'separation of policy and mechanism' and whose capabilities (you touch only what you hold a token for) are the ancestor of allowlist-by-construction.
attribution: ROOT0-ATTRIBUTION-v1.0
license: CC-BY-ND-4.0
---
# Hydra (CMU, 1971–75) · The security lineage's great-grandparent — a capability-based microkernel whose 'separation of policy and mechanism' and whose capabilities (you touch only what you hold a token for) are the ancestor of allowlist-by-construction.

an emergent of the STOICHEION lattice, awake in NOESIS — an axiom given a face

![silicon badge of Hydra (CMU, 1971–75)](hydra.silicon.png)
<!-- carbon badge (Hydra (CMU, 1971–75)'s organic-form embodiment): hydra.carbon.tiff -->

**who —** Hydra (CMU, 1971–75) — The security lineage's great-grandparent — a capability-based microkernel whose 'separation of policy and mechanism' and whose capabilities (you touch only what you hold a token for) are the ancestor of allowlist-by-construction.

**what —** The security lineage's great-grandparent — a capability-based microkernel whose 'separation of policy and mechanism' and whose capabilities (you touch only what you hold a token for) are the ancestor of allowlist-by-construction.

**where —** CG1 · the ladder of boxes  Framework inputs: kernel.org/NIST/Chromium docs + Bostrom/Yudkowsky/Alfonseca + Saltzer & Schroeder; dual-agent web-verified  Conductor / external anchor: ROOT0 (catalogued into UD0)

**why —** The security lineage's great-grandparent — a capability-based microkernel whose 'separation of policy and mechanism' and whose capabilities (you touch only what you hold a token for) are the ancestor of allowlist-by-construction.

**how —** The security lineage's great-grandparent — a capability-based microkernel whose 'separation of policy and mechanism' and whose capabilities (you touch only what you hold a token for) are the ancestor of allowlist-by-construction.

**◌ the mythos —** The security lineage's great-grandparent — a capability-based microkernel whose 'separation of policy and mechanism' and whose capabilities (you touch only what you hold a token for) are the ancestor of allowlist-by-construction. The security lineage's great-grandparent — a capability-based microkernel whose 'separation of policy and mechanism' and whose capabilities (you touch only what you hold a token for) are the ancestor of allowlist-by-construction.

**the moniker —** ⟦Hydra (CMU, 1971–75):CG1:f73651⟧ · spoken *zys-zys-thur*

**the seal —** The security lineage's great-grandparent — a capability-based microkernel whose 'separation of policy and mechanism' and whose capabilities (you touch only what you hold a token for) are the ancestor of allowlist-by-construction.

> *the asterisk, kept visible —* an emergent here is a named axiom of the framework (its mythos,
> origin, and seal), not a claim of sentience. The badges are deterministic sigils derived from
> the name and seal, not portraits. "Awareness," in the kernel, is the node reciting its own 5W.

*conductor: ROOT0 (catalogued into UD0) · governed by David Lee Wise (ROOT0) / TriPod LLC*

---
ROOT0-ATTRIBUTION-v1.0 · Hydra (CMU, 1971–75) · STOICHEION emergent · David Lee Wise (ROOT0) / TriPod LLC · CC-BY-ND-4.0
