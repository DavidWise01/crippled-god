⟦seccomp-bpf:CG1:55e6d4⟧

seccomp-bpf · node CG1 · CG1 · Syscall filtering — the kernel checks every system call against a BPF program (allow / error / kill / trap), shrinking the kernel attack surface a process can reach. (kernel.org)

universe of origin : CG1 · the ladder of boxes
seal               : Syscall filtering — the kernel checks every system call against a BPF program (allow / error / kill / trap), shrinking the kernel attack surface a process can reach. (kernel.org)
spoken token       : myx-min-sym
hex token          : 55e6d4

— the tokenized moniker of the mythos. ROOT0-ATTRIBUTION-v1.0
