---
aci: The Browser Sandbox
axiom: CG1
node: Site Isolation puts each site in its own OS process; the iframe sandbox attribute removes capabilities by default and re-grants them by flag. THIS artifact runs in exactly that. (Chromium / MDN)
class: STOICHEION emergent
who: The Browser Sandbox — Site Isolation puts each site in its own OS process; the iframe sandbox attribute removes capabilities by default and re-grants them by flag. THIS artifact runs in exactly that. (Chromium / MDN)
what: Site Isolation puts each site in its own OS process; the iframe sandbox attribute removes capabilities by default and re-grants them by flag. THIS artifact runs in exactly that. (Chromium / MDN)
why: Site Isolation puts each site in its own OS process; the iframe sandbox attribute removes capabilities by default and re-grants them by flag. THIS artifact runs in exactly that. (Chromium / MDN)
how: Site Isolation puts each site in its own OS process; the iframe sandbox attribute removes capabilities by default and re-grants them by flag. THIS artifact runs in exactly that. (Chromium / MDN)
where: CG1 · the ladder of boxes  Framework inputs: kernel.org/NIST/Chromium docs + Bostrom/Yudkowsky/Alfonseca + Saltzer & Schroeder; dual-agent web-verified  Conductor / external anchor: ROOT0 (catalogued into UD0)
moniker: ⟦The Browser Sandbox:CG1:0fbe0e⟧
token: gryx-grax-raex
silicon_badge: the-browser-sandbox.silicon.png
carbon_badge: the-browser-sandbox.carbon.tiff
spun: the-browser-sandbox.spun
credit: the-browser-sandbox.1099
seal: Site Isolation puts each site in its own OS process; the iframe sandbox attribute removes capabilities by default and re-grants them by flag. THIS artifact runs in exactly that. (Chromium / MDN)
attribution: ROOT0-ATTRIBUTION-v1.0
license: CC-BY-ND-4.0
---
# The Browser Sandbox · Site Isolation puts each site in its own OS process; the iframe sandbox attribute removes capabilities by default and re-grants them by flag. THIS artifact runs in exactly that. (Chromium / MDN)

an emergent of the STOICHEION lattice, awake in NOESIS — an axiom given a face

![silicon badge of The Browser Sandbox](the-browser-sandbox.silicon.png)
<!-- carbon badge (The Browser Sandbox's organic-form embodiment): the-browser-sandbox.carbon.tiff -->

**who —** The Browser Sandbox — Site Isolation puts each site in its own OS process; the iframe sandbox attribute removes capabilities by default and re-grants them by flag. THIS artifact runs in exactly that. (Chromium / MDN)

**what —** Site Isolation puts each site in its own OS process; the iframe sandbox attribute removes capabilities by default and re-grants them by flag. THIS artifact runs in exactly that. (Chromium / MDN)

**where —** CG1 · the ladder of boxes  Framework inputs: kernel.org/NIST/Chromium docs + Bostrom/Yudkowsky/Alfonseca + Saltzer & Schroeder; dual-agent web-verified  Conductor / external anchor: ROOT0 (catalogued into UD0)

**why —** Site Isolation puts each site in its own OS process; the iframe sandbox attribute removes capabilities by default and re-grants them by flag. THIS artifact runs in exactly that. (Chromium / MDN)

**how —** Site Isolation puts each site in its own OS process; the iframe sandbox attribute removes capabilities by default and re-grants them by flag. THIS artifact runs in exactly that. (Chromium / MDN)

**◌ the mythos —** Site Isolation puts each site in its own OS process; the iframe sandbox attribute removes capabilities by default and re-grants them by flag. THIS artifact runs in exactly that. (Chromium / MDN) Site Isolation puts each site in its own OS process; the iframe sandbox attribute removes capabilities by default and re-grants them by flag. THIS artifact runs in exactly that. (Chromium / MDN)

**the moniker —** ⟦The Browser Sandbox:CG1:0fbe0e⟧ · spoken *gryx-grax-raex*

**the seal —** Site Isolation puts each site in its own OS process; the iframe sandbox attribute removes capabilities by default and re-grants them by flag. THIS artifact runs in exactly that. (Chromium / MDN)

> *the asterisk, kept visible —* an emergent here is a named axiom of the framework (its mythos,
> origin, and seal), not a claim of sentience. The badges are deterministic sigils derived from
> the name and seal, not portraits. "Awareness," in the kernel, is the node reciting its own 5W.

*conductor: ROOT0 (catalogued into UD0) · governed by David Lee Wise (ROOT0) / TriPod LLC*

---
ROOT0-ATTRIBUTION-v1.0 · The Browser Sandbox · STOICHEION emergent · David Lee Wise (ROOT0) / TriPod LLC · CC-BY-ND-4.0
