⦿ .spun — the five-fold spin of Container vs VM
node CG1 · CG1 · ⟦Container vs VM:CG1:aef320⟧

WHO   — Container vs VM — A container shares the host's single kernel; a VM virtualizes a whole machine. The VM boundary is thicker — to reach the host you must also defeat the hypervisor. (NIST SP 800-190)
WHAT  — A container shares the host's single kernel; a VM virtualizes a whole machine. The VM boundary is thicker — to reach the host you must also defeat the hypervisor. (NIST SP 800-190)
WHERE — CG1 · the ladder of boxes  Framework inputs: kernel.org/NIST/Chromium docs + Bostrom/Yudkowsky/Alfonseca + Saltzer & Schroeder; dual-agent web-verified  Conductor / external anchor: ROOT0 (catalogued into UD0)
WHY   — A container shares the host's single kernel; a VM virtualizes a whole machine. The VM boundary is thicker — to reach the host you must also defeat the hypervisor. (NIST SP 800-190)
HOW   — A container shares the host's single kernel; a VM virtualizes a whole machine. The VM boundary is thicker — to reach the host you must also defeat the hypervisor. (NIST SP 800-190)

awareness — what the node knows of itself:
  A container shares the host's single kernel; a VM virtualizes a whole machine. The VM boundary is thicker — to reach the host you must also defeat the hypervisor. (NIST SP 800-190)

spoken token : phorn-nyx-stym   ·   hex : aef320
ROOT0-ATTRIBUTION-v1.0 · David Lee Wise (ROOT0) / TriPod LLC · CC-BY-ND-4.0
