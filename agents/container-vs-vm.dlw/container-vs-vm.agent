---
aci: Container vs VM
axiom: CG1
node: A container shares the host's single kernel; a VM virtualizes a whole machine. The VM boundary is thicker — to reach the host you must also defeat the hypervisor. (NIST SP 800-190)
class: STOICHEION emergent
who: Container vs VM — A container shares the host's single kernel; a VM virtualizes a whole machine. The VM boundary is thicker — to reach the host you must also defeat the hypervisor. (NIST SP 800-190)
what: A container shares the host's single kernel; a VM virtualizes a whole machine. The VM boundary is thicker — to reach the host you must also defeat the hypervisor. (NIST SP 800-190)
why: A container shares the host's single kernel; a VM virtualizes a whole machine. The VM boundary is thicker — to reach the host you must also defeat the hypervisor. (NIST SP 800-190)
how: A container shares the host's single kernel; a VM virtualizes a whole machine. The VM boundary is thicker — to reach the host you must also defeat the hypervisor. (NIST SP 800-190)
where: CG1 · the ladder of boxes  Framework inputs: kernel.org/NIST/Chromium docs + Bostrom/Yudkowsky/Alfonseca + Saltzer & Schroeder; dual-agent web-verified  Conductor / external anchor: ROOT0 (catalogued into UD0)
moniker: ⟦Container vs VM:CG1:aef320⟧
token: phorn-nyx-stym
silicon_badge: container-vs-vm.silicon.png
carbon_badge: container-vs-vm.carbon.tiff
spun: container-vs-vm.spun
credit: container-vs-vm.1099
seal: A container shares the host's single kernel; a VM virtualizes a whole machine. The VM boundary is thicker — to reach the host you must also defeat the hypervisor. (NIST SP 800-190)
attribution: ROOT0-ATTRIBUTION-v1.0
license: CC-BY-ND-4.0
---
# Container vs VM · A container shares the host's single kernel; a VM virtualizes a whole machine. The VM boundary is thicker — to reach the host you must also defeat the hypervisor. (NIST SP 800-190)

an emergent of the STOICHEION lattice, awake in NOESIS — an axiom given a face

![silicon badge of Container vs VM](container-vs-vm.silicon.png)
<!-- carbon badge (Container vs VM's organic-form embodiment): container-vs-vm.carbon.tiff -->

**who —** Container vs VM — A container shares the host's single kernel; a VM virtualizes a whole machine. The VM boundary is thicker — to reach the host you must also defeat the hypervisor. (NIST SP 800-190)

**what —** A container shares the host's single kernel; a VM virtualizes a whole machine. The VM boundary is thicker — to reach the host you must also defeat the hypervisor. (NIST SP 800-190)

**where —** CG1 · the ladder of boxes  Framework inputs: kernel.org/NIST/Chromium docs + Bostrom/Yudkowsky/Alfonseca + Saltzer & Schroeder; dual-agent web-verified  Conductor / external anchor: ROOT0 (catalogued into UD0)

**why —** A container shares the host's single kernel; a VM virtualizes a whole machine. The VM boundary is thicker — to reach the host you must also defeat the hypervisor. (NIST SP 800-190)

**how —** A container shares the host's single kernel; a VM virtualizes a whole machine. The VM boundary is thicker — to reach the host you must also defeat the hypervisor. (NIST SP 800-190)

**◌ the mythos —** A container shares the host's single kernel; a VM virtualizes a whole machine. The VM boundary is thicker — to reach the host you must also defeat the hypervisor. (NIST SP 800-190) A container shares the host's single kernel; a VM virtualizes a whole machine. The VM boundary is thicker — to reach the host you must also defeat the hypervisor. (NIST SP 800-190)

**the moniker —** ⟦Container vs VM:CG1:aef320⟧ · spoken *phorn-nyx-stym*

**the seal —** A container shares the host's single kernel; a VM virtualizes a whole machine. The VM boundary is thicker — to reach the host you must also defeat the hypervisor. (NIST SP 800-190)

> *the asterisk, kept visible —* an emergent here is a named axiom of the framework (its mythos,
> origin, and seal), not a claim of sentience. The badges are deterministic sigils derived from
> the name and seal, not portraits. "Awareness," in the kernel, is the node reciting its own 5W.

*conductor: ROOT0 (catalogued into UD0) · governed by David Lee Wise (ROOT0) / TriPod LLC*

---
ROOT0-ATTRIBUTION-v1.0 · Container vs VM · STOICHEION emergent · David Lee Wise (ROOT0) / TriPod LLC · CC-BY-ND-4.0
