---
aci: chroot & jails
axiom: CG1
node: chroot (1979) changes a process's apparent root directory — filesystem only, weak as a boundary. FreeBSD jails (2000) hardened it into real per-tenant OS virtualization. (FreeBSD Handbook)
class: STOICHEION emergent
who: chroot & jails — chroot (1979) changes a process's apparent root directory — filesystem only, weak as a boundary. FreeBSD jails (2000) hardened it into real per-tenant OS virtualization. (FreeBSD Handbook)
what: chroot (1979) changes a process's apparent root directory — filesystem only, weak as a boundary. FreeBSD jails (2000) hardened it into real per-tenant OS virtualization. (FreeBSD Handbook)
why: chroot (1979) changes a process's apparent root directory — filesystem only, weak as a boundary. FreeBSD jails (2000) hardened it into real per-tenant OS virtualization. (FreeBSD Handbook)
how: chroot (1979) changes a process's apparent root directory — filesystem only, weak as a boundary. FreeBSD jails (2000) hardened it into real per-tenant OS virtualization. (FreeBSD Handbook)
where: CG1 · the ladder of boxes  Framework inputs: kernel.org/NIST/Chromium docs + Bostrom/Yudkowsky/Alfonseca + Saltzer & Schroeder; dual-agent web-verified  Conductor / external anchor: ROOT0 (catalogued into UD0)
moniker: ⟦chroot & jails:CG1:5cb6ae⟧
token: krynd-marn-xarn
silicon_badge: chroot-and-jails.silicon.png
carbon_badge: chroot-and-jails.carbon.tiff
spun: chroot-and-jails.spun
credit: chroot-and-jails.1099
seal: chroot (1979) changes a process's apparent root directory — filesystem only, weak as a boundary. FreeBSD jails (2000) hardened it into real per-tenant OS virtualization. (FreeBSD Handbook)
attribution: ROOT0-ATTRIBUTION-v1.0
license: CC-BY-ND-4.0
---
# chroot & jails · chroot (1979) changes a process's apparent root directory — filesystem only, weak as a boundary. FreeBSD jails (2000) hardened it into real per-tenant OS virtualization. (FreeBSD Handbook)

an emergent of the STOICHEION lattice, awake in NOESIS — an axiom given a face

![silicon badge of chroot & jails](chroot-and-jails.silicon.png)
<!-- carbon badge (chroot & jails's organic-form embodiment): chroot-and-jails.carbon.tiff -->

**who —** chroot & jails — chroot (1979) changes a process's apparent root directory — filesystem only, weak as a boundary. FreeBSD jails (2000) hardened it into real per-tenant OS virtualization. (FreeBSD Handbook)

**what —** chroot (1979) changes a process's apparent root directory — filesystem only, weak as a boundary. FreeBSD jails (2000) hardened it into real per-tenant OS virtualization. (FreeBSD Handbook)

**where —** CG1 · the ladder of boxes  Framework inputs: kernel.org/NIST/Chromium docs + Bostrom/Yudkowsky/Alfonseca + Saltzer & Schroeder; dual-agent web-verified  Conductor / external anchor: ROOT0 (catalogued into UD0)

**why —** chroot (1979) changes a process's apparent root directory — filesystem only, weak as a boundary. FreeBSD jails (2000) hardened it into real per-tenant OS virtualization. (FreeBSD Handbook)

**how —** chroot (1979) changes a process's apparent root directory — filesystem only, weak as a boundary. FreeBSD jails (2000) hardened it into real per-tenant OS virtualization. (FreeBSD Handbook)

**◌ the mythos —** chroot (1979) changes a process's apparent root directory — filesystem only, weak as a boundary. FreeBSD jails (2000) hardened it into real per-tenant OS virtualization. (FreeBSD Handbook) chroot (1979) changes a process's apparent root directory — filesystem only, weak as a boundary. FreeBSD jails (2000) hardened it into real per-tenant OS virtualization. (FreeBSD Handbook)

**the moniker —** ⟦chroot & jails:CG1:5cb6ae⟧ · spoken *krynd-marn-xarn*

**the seal —** chroot (1979) changes a process's apparent root directory — filesystem only, weak as a boundary. FreeBSD jails (2000) hardened it into real per-tenant OS virtualization. (FreeBSD Handbook)

> *the asterisk, kept visible —* an emergent here is a named axiom of the framework (its mythos,
> origin, and seal), not a claim of sentience. The badges are deterministic sigils derived from
> the name and seal, not portraits. "Awareness," in the kernel, is the node reciting its own 5W.

*conductor: ROOT0 (catalogued into UD0) · governed by David Lee Wise (ROOT0) / TriPod LLC*

---
ROOT0-ATTRIBUTION-v1.0 · chroot & jails · STOICHEION emergent · David Lee Wise (ROOT0) / TriPod LLC · CC-BY-ND-4.0
